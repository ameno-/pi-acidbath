# Handoff — Acidbath Eval & Perf, Session 2 (takeover)

Paste-ready for a new AI session. Supersedes `docs/HANDOFF-eval.md`. Continues `docs/PLAN.md`.

## Session 2 continuation (2026-08-07)

The remaining design deliverables and approval gates were continued after this
handoff was written:

- **A3 completed and approved:** the eight dead package entries were removed
  from `~/.pi/agent/settings.json`; visual-explainer's empty surface filters
  were removed. Global blocklists and the five-skill allowlist were unchanged.
- **D completed as design:** `docs/skills-lib-integration.md` was added and
  PLAN §6.1 now treats `/Users/ameno/dev/lib` as canonical. No lib registry or
  skill files were changed; the `pi` platform and mirror-vs-direct choice
  remain explicit design gates.
- **P1 labels approved and wired:** `ui-labels.ts` now feeds `apply()` with a
  100ms trailing debounce and same-message churn guard; tool renderers provide
  render-time label signals. UI-label tests still pass (1103/1103).
- **ast-grep approved and configured:** settings now pins
  `code-yeongyu/pi-ast-grep` to `4a7d1beee684d96a6890e5fc55710bb63fecca85`.
  `cargo install ast-grep --locked` installed 0.45.0; paired
  `/opt/homebrew/bin/ast-grep` and `/opt/homebrew/bin/sg` symlinks expose the
  Cargo binaries to the resolver. `PI_OFFLINE=1` remains mandatory at Pi
  launch.
- **Still uncommitted:** all repo changes remain uncommitted. Do not apply the
  old “do not wire labels” gate; the human explicitly approved it in this
  continuation. The lib integration and commit strategy still need a human
  decision.

## Mission (unchanged)

Evaluation and performance work for the `acidbath` Pi extension umbrella. Perf/eval/security focused. **Visual/UI changes (orb, motion, gauge, label rendering) remain deferred — the human decides those.** Session 1 instrumented and measured them but did not change their look/behavior.

## State of the world

Session 1 completed workstreams **A1 (baselines), A2 (V1 labels prototype), B (tool-eval matrix), C (ast-grep supply-chain review)**. Remaining: **A3 (settings diff proposal), D (skills↔lib design), and all human-approval gates.**

**Nothing is committed.** `git status`: `?? docs/` (entire dir, including PLAN.md from session 0), `?? scripts/`, `?? extensions/acidbath/ui-labels.ts`. Only commit on `main` is `4b02754 chore: bootstrap acidbath package`. First action this session should be deciding what to commit.

## Completed deliverables (uncommitted)

| File | What | Verification |
|---|---|---|
| `docs/baselines.md` | Phase 0 measured baselines | 15/15 negative-space checks pass |
| `docs/bench-results/current.json` + timestamped run | Machine-readable bench output | regenerated by bench script |
| `scripts/bench-tool-render.mjs` | Perf harness (B1–B5) | `node --experimental-strip-types --no-warnings scripts/bench-tool-render.mjs` → exit 0 |
| `docs/tool-eval-matrix.md` | Scored matrix, 26 rows, 6 columns (compat/maint/license/security/perf/op-risk) | read-only analysis |
| `docs/ast-grep-security-review.md` | Supply-chain finding + adopt/reject + controls | source-verified via /tmp clones |
| `extensions/acidbath/ui-labels.ts` | V1 deterministic label synthesis, **pure + unconnected** | NOT imported by `index.ts`, NOT wired into `apply()` |
| `scripts/test-ui-labels.mjs` | Unit tests for ui-labels | **1103 passed, 0 failed** |

## Key results

**Baselines (Node v24.17.0, darwin-arm64):** all pure-helper paths are 3–4 orders of magnitude under the PLAN §5 budget of <0.5ms/call. `stateForTool`+label mean 0.03µs; `indicatorFor` 0.12µs; `buildGaugeLine(w=80)` 0.31µs; MotionClock sub/unsub 1.7µs; ToolLifecycle render 0.47µs. **0 active timers at idle** (MotionClock clears when no pending subscribers; lifecycle wrapper unsubscribes on result). 20-event burst with churn-guard pattern → 6 unique labels, 0 redundant `setWorkingMessage` calls. Conclusion: current code is clean; the only P1 work is wiring the churn guard + V1 labels into `apply()` (needs approval).

**ast-grep recommendation (PLAN-ONLY, awaiting human approval):** Adopt `code-yeongyu/pi-ast-grep` pinned to commit `4a7d1beee684d96a6890e5fc55710bb63fecca85` with 5 mandatory controls:
- C1: pin to commit SHA (no npm release exists)
- C2: `PI_OFFLINE=1` (disables the no-checksum GitHub auto-download — the extension's only network path, `src/ast-grep/binary-downloader.ts:22`)
- C3: `brew install ast-grep` (Homebrew formula hard-verifies SHA256) or `cargo install ast-grep --locked`
- C4: code-yeongyu does NOT honor `AST_GREP_BIN`; resolver checks `/opt/homebrew/bin/sg` at step 5 of 6 — Homebrew install satisfies it
- C5: after any install, `cat node_modules/@ast-grep/cli/postinstall.js` to confirm it's still the benign PR-#2595 shim
- Deferred fallback: `bjoernaagaard/pi-ast-grep` (safer defaults, honors `AST_GREP_BIN`, but 8 commits / 0★ / 1 contributor). Rejected: `can1357/oh-my-pi` ast-grep.ts (imports 7 `@oh-my-pi/*` packages — wrong runtime scope).

**Tool-eval matrix verdicts:** Adopt now — pi-subagents, pi-codex-compaction, pix-bash pattern, pi-lens (debug-gated), pi-autoresearch, visual-explainer (after unblock). Adopt with controls — pi-codex-tools, pi-ast-grep. Defer — bjoernaagaard, pi-minimax-mcp (needs `@mariozechner→@earendil-works` peer-dep migration), my-pi-setup. Drop (dead/broken `packages` entries) — session-replay, damage-control, tool-status (missing `.ts` file), council (malformed manifest `pi.extensions: ["extensions"]`), pi-screenshots-picker, @plannotator/pi-extension, pi-ask, plus agy / research-bridge / web_search / pi-herdr-ui (already rejected in PLAN §0/§8).

## Remaining work (this session's job)

1. **A3 — Propose (do NOT apply) `~/.pi/agent/settings.json` diff** → `docs/proposed-settings-diff.md`:
   - Remove 8 dead entries: `npm:pi-screenshots-picker@1.2.2`, `/Users/ameno/dev/pi-minimax-mcp`, `~/.pi/agent/extensions/session-replay`, `~/.pi/agent/extensions/damage-control`, `~/.pi/agent/extensions/tool-status`, `/Users/ameno/dev/pi-ext/.pi/council`, `npm:@plannotator/pi-extension@0.25.0`, `https://github.com/eko24ive/pi-ask@2bba854`.
   - Unblock visual-explainer: its entry is `{source: "https://github.com/nicobailon/visual-explainer@528b71f", extensions: [], skills: [], prompts: []}` — the **empty arrays are the filter** (explicit per-package surface override). Fix = remove the empty arrays so the manifest's declared surfaces load, or replace with the real surface paths. Verify the package's on-disk location first (session-1 `find` searches timed out; try `~/.pi/agent/npm/node_modules/` and pi's package cache).
   - Keep all blocklists (`!extensions/**`, `!prompts/**`, `!skills/**` + 5-skill allowlist) untouched.
2. **D — Skills↔lib integration design** → `docs/skills-lib-integration.md` + revise PLAN.md §6.1. Canonical skill source is `~/dev/lib/skills` (NOT `acidbath/skills` — do not duplicate authorship). Lib facts: `registry.yaml` (58 skills, per-skill `build_targets`), `scripts/skills` CLI (`build` → `dist/<platform>/skills/` symlink mirrors; `deploy <platform>` → symlinks into `~/.claude/skills` etc.; `scope` for session-scoped deploys), `skills.lock` with sha256 digests. Open design question (from session-0 handoff, still unanswered): should acidbath's `pi.skills` manifest point at lib's `dist/pi/` mirror, or symlink directly to `~/dev/lib/skills/<name>`? Note: lib's `platforms:` registry currently lists codex/claude/factory/antigravity — **no `pi` platform entry exists yet**; adding one is part of the design.
3. **Open questions for the human** (surface, don't guess):
   - Approve ast-grep adoption (code-yeongyu @ 4a7d1be + C1–C5)? Confirm `brew install ast-grep` as the manual binary source on this host.
   - Approve wiring `ui-labels.ts` into `apply()` with churn guard + 100ms trailing-edge debounce (P1)? The prototype + tests are ready.
   - Approve applying the settings.json diff (A3) once proposed?
   - Lib integration: `dist/pi/` mirror vs direct symlinks in `pi.skills` manifest?
   - Commit strategy for the untracked session-1 output (all of `docs/`, `scripts/`, `ui-labels.ts`).

## Hard constraints (unchanged, still binding)

- PLAN-ONLY for ast-grep selection and eval-rubric adoption until the human approves. Perf instrumentation is built and allowed to run.
- No secret/auth/token file reads. No exfiltration.
- Keep Pi global discovery blocked. Never load `pi-herdr-ui.ts` with acidbath (collides on `/orb`, `/motion`, gauge).
- No extension that auto-downloads executables without checksums unless mitigating controls are mandated (hence C2/C3).
- Skills canonical source is `~/dev/lib`, NOT `acidbath/skills`.
- Do not wire `ui-labels.ts` into `apply()` or change any UI behavior without explicit approval.
- Settings diff is propose-then-apply, never apply-first.
- Separate facts from assumptions; cite file paths or repo URLs.

## Environment gotchas (learned the hard way)

- **Node v24.17.0** (`/Users/ameno/.nvm/versions/node/v24.17.0/bin/node`). `--experimental-strip-types` runs the repo's `.ts` directly, BUT in `.mjs` scripts it rejects `interface` declarations and `import type` — keep bench/test scripts type-annotation-free JS that imports the real `.ts` modules. Both session-1 scripts follow this.
- `tsx`/`ts-node` are NOT installed; no `node_modules` in acidbath. Don't add a build step.
- Broad `find /` searches time out on this host — scope searches to `~/.pi`, `~/dev`, `~/.npm`.
- `~/.pi/agent/extensions/` contains legacy loose files (`handoff.ts`, `herdr-agent-state.ts`, `tool-status.ts` at the ROOT, not in the tool-status package dir) plus `pi-autoresearch/`, `subagent-widget/` dirs not referenced in settings — all dead under the blocklist.
- Pi global skills dir has ~30 skills installed; only 5 allowlisted (`agent-browser`, `gotem`, `herdr`, `mmx-cli`, `react-native-dev`).

## Verified environment context (source of truth, unchanged)

- Acidbath repo: `/Users/ameno/dev/acidbath` — `pi.extensions: ["./extensions/acidbath/index.ts"]`, `pi.themes: ["./themes/*.json"]`, peer deps `@earendil-works/pi-*`.
- Pi global settings: `~/.pi/agent/settings.json` — `defaultProvider: ap-copilot`, `defaultModel: copilot-gpt-5.3-codex`, `defaultThinkingLevel: high`, `hideThinkingBlock: true`, `transport: sse`. Active packages: acidbath + `npm:pi-interactive-shell@0.14.0`.
- Pi-Herdr repo: `/Users/ameno/dev/pi-herdr` — loads only via `scripts/launcher.sh` in a separate pane. Heritage to vendor (P3): **scheduler + cockpit only** (agy + research-bridge dropped).
- Lib repo: `/Users/ameno/dev/lib` — canonical skills/agents ecosystem (`registry.yaml`, `skills.lock`, `scripts/skills` CLI, `build-skills.py`, `validate-skills.py`, `collections/`, `skills-as-deps/`).
- Round-2 locked decisions: PLAN.md §0 (pi-subagents is the delegation surface; web_search dropped; apply_patch kept/macOS-inactive; pi-lens debug-scoped; scored-matrix-only eval first pass; eval profile freezes motion with `PI_ACIDBATH_REDUCED_MOTION=1`, `PHASE=3`).

## Quick resume commands

```bash
cd /Users/ameno/dev/acidbath
node --experimental-strip-types --no-warnings scripts/bench-tool-render.mjs   # baselines, exit 0
node --experimental-strip-types --no-warnings scripts/test-ui-labels.mjs      # 1103 pass
git status --short                                                            # all untracked
```
